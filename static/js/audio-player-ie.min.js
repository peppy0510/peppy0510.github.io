function initAudioPlayer(url){let audioPlayerElement=$('.audio-player')[0];let audio=document.createElement('audio');audio.controls='controls';audioPlayerElement.appendChild(audio);let audioElement=$('.audio-player audio')[0];let source=document.createElement('source');source.src=url;source.type='audio/mpeg';source.preload='auto';audioElement.appendChild(source);};